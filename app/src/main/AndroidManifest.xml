<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.linyi.viva">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />


    <application
        tools:replace="android:name"
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity"
            android:launchMode="singleTask"/>

        <activity android:name=".ui.activity.ClassifyActivity"/>
        <activity android:name=".ui.activity.Classify2Activity"/>
        <activity android:name=".ui.activity.BookDetailActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".ui.activity.RankActivity"/>
        <activity android:name=".ui.activity.ReadCatalogActivity"/>
        <activity android:name=".ui.activity.SearchActivity"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".ui.activity.DiscoverActivity"/>
        <activity android:name=".ui.activity.DiscoverDetailActivity"/>
        <activity android:name=".ui.activity.ExchangeRecordsActivity"/>
        <activity android:name=".ui.activity.HistoryRecordActivity"/>
    </application>

</manifest>